{
    "title": "Malaysia Clinic Scraper Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "startUrls": {
            "title": "Start URLs",
            "type": "array",
            "description": "List of search URLs to scrape.",
            "editor": "requestListSources",
            "prefill": [
                { "url": "https://www.clinicadvisor.com/malaysia/kuala-lumpur/clinics" }
            ]
        },
        "maxLeads": {
            "title": "Maximum Leads",
            "type": "integer",
            "description": "Maximum number of Malaysia clinic leads to extract.",
            "default": 100,
            "minimum": 1
        },
        "proxyConfiguration": {
            "title": "Proxy Configuration",
            "type": "object",
            "description": "Proxy settings. Apify Residential Proxy (Malaysia targeted) is highly required.",
            "prefill": { "useApifyProxy": true },
            "editor": "proxy"
        }
    },
    "required": ["startUrls"]
}
